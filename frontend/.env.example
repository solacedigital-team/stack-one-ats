# This file is for user reference.
# Replace the placeholders with your actual credentials and values.
# Copy the contents of this file into a `.env` file and replace the example values with your actual configurations.

# Base URL for the backend API calls
# Replace "http://localhost:3001" with your actual backend API base URL.
REACT_APP_API_BASE_URL="http://localhost:3001"

# Example of another environment variable:
# Replace "https://example.com" with the actual URL for another API or service if needed.
# REACT_APP_ANOTHER_API_URL="https://example.com"

# Ensure that you keep your `.env` file out of version control to avoid exposing sensitive information.
# Create a `.env` file in the root of your project and populate it with the values from this file.
# Example: 
# cp .env.example .env
# Then edit `.env` with your actual values.
